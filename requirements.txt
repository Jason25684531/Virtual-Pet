# Linux 的 libxcb / libegl 等屬於系統層 runtime，需用套件管理器安裝，不在 pip 清單中替代這些 Python 套件。
PyQt5==5.15.11
PyQtWebEngine==5.15.7
requests==2.33.1
websocket-client==1.9.0
python-dotenv==1.2.2
pygame==2.6.1
pytest>=9.1
playwright>=1.55
beautifulsoup4>=4.13
feedparser>=6.0
qdrant-client>=1.13
fastembed>=0.5
jieba>=0.42
unidecode>=1.3  # 中文角色名音譯成 character_id slug（char-ya-zhou-tong-shen）
onnxruntime>=1.27
numpy>=1.26
sounddevice>=0.5
faster-whisper>=1.2
# Windows 上 ctranslate2 執行 CUDA 推論需要的 cuBLAS/cuDNN runtime DLL；系統已裝
# CUDA Toolkit 者可略過，但這裡明確宣告以避免 "cublas64_12.dll is not found" 錯誤。
nvidia-cublas-cu12; sys_platform == "win32"
nvidia-cudnn-cu12; sys_platform == "win32"
