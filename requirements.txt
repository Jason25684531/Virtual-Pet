# Linux 的 libxcb / libegl 等屬於系統層 runtime，需用套件管理器安裝，不在 pip 清單中替代這些 Python 套件。
PyQt5==5.15.11
PyQtWebEngine==5.15.7
opencv-python>=4.10
requests==2.33.1
websocket-client==1.9.0
websockets==16.0
cryptography==46.0.7
langchain==0.3.27
langchain-community==0.3.31
langchain-openai==0.3.35
python-dotenv==1.2.2
azure-cognitiveservices-speech==1.46.0
pygame==2.6.1
pytest>=9.1
playwright>=1.55
beautifulsoup4>=4.13
feedparser>=6.0
qdrant-client>=1.13
fastembed>=0.5
numpy>=1.26
sounddevice>=0.5
faster-whisper>=1.2
# Windows 上 ctranslate2 執行 CUDA 推論需要的 cuBLAS/cuDNN runtime DLL；系統已裝
# CUDA Toolkit 者可略過，但這裡明確宣告以避免 "cublas64_12.dll is not found" 錯誤。
nvidia-cublas-cu12; sys_platform == "win32"
nvidia-cudnn-cu12; sys_platform == "win32"
