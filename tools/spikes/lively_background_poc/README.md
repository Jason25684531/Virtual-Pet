# ECHOES 背景專用 Lively 壁紙

這個套件只顯示背景圖，預設使用較深色的 `BG_Final.png`。角色、對話、語音、動作與 UI 仍由主程式處理。

請將 `lively-echoes-background-poc.zip` 拖入 Lively 桌布庫並套用。主程式預設會同步該角色的背景圖；若 `.env` 已設定 `LIVELY_BACKGROUND_ENABLED=false`，請移除或改成 `true`。
